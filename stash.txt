git stash
misal jika ingin membangun sebuah fitur baru di branch feature, tiba" teman kalian bilang untuk switch ke master/main untuk fix masalah.
nah kita butuh fungsi git stash ingin

intinya bisa pindah ke branch master/main tanpa harus melakukan commit dahulu dibranch feature
saat kembali lagi ke branch feature maka script yang sudah kalian buat dibranch feature tidak akan hilang walaupun tidak dicommit
git stash
git stash pop
git stash apply

cara membalikan source code yang terakhir buat
git stash pop untuk yang terakhir dan menghapus snapshot yang lainnya
git stash apply untuk snapshot yang banyakk


git push -u origin feature

cara menghapus stash
git stash drop

git stash apply __namastash__
yang paling atas adalah yang paling terakhir diupdate


git merge --squash __branch_

git log --oneline